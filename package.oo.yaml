version: 0.0.3
icon: ./icon.png
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    set -e
    poetry install --no-root
dependencies:
  oomol-preview: 0.0.4
name: azure-speech
description: Use Azure AI Speech to synthesize the input text into a human-like voice.
displayName: Azure Speech
repository: https://github.com/oomol-flows/azure-speech
releaseNotes: upgrade depdencies.
